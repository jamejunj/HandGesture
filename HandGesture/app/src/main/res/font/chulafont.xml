<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- regular -->
    <font
        android:font="@font/ccn"
        android:fontStyle="normal"
        android:fontWeight="400"

        app:font="@font/ccn"
        app:fontStyle="normal"
        app:fontWeight="400" />

    <!-- italic -->
    <font
        android:font="@font/ccni"
        android:fontStyle="italic"
        android:fontWeight="400"

        app:font="@font/ccni"
        app:fontStyle="italic"
        app:fontWeight="400" />

    <!-- bold -->
    <font
        android:font="@font/ccnb"
        android:fontStyle="normal"
        android:fontWeight="700"

        app:font="@font/ccnb"
        app:fontStyle="normal"
        app:fontWeight="700" />

    <!-- bold itatic -->
    <font
        android:font="@font/ccnbi"
        android:fontStyle="italic"
        android:fontWeight="700"

        app:font="@font/ccnb"
        app:fontStyle="italic"
        app:fontWeight="700" />

</font-family>